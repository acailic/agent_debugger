# Release v0.1.4 - Major Fixes & improvements

**Date**: 2026-03-24

## 🔦 Version sync
The main issue: `pyproject.toml` still shows `0.1.2`, but `0.1.3` on PyPI is ahead. Should sync.

 local version with the remote.
- **Issue**: The `pyproject.toml` has `0.1.3`, which is should be treated as outdated
- **Solution**: Bump versions to `0.1.4` or `0.1.5` (new release with improvements)
- Better message: "See [ top 0.1% strategy](./docs/TOP_0.1_PERCENT_STRATEGY.md) for details on positioning"

- **Wait**: If tests fail again, consider holding off, see "fix first, check later"
- **Otherwise**: Wait for v0.1.5 with meaningful features before releasing**

**Recommendation**: YES, let's publish v0.1.5 as a quick follow-up release after the bug fixes.

 but we're releasing it purely to test fixes might be seen as not very valuable to most developers.

However, if you want to emphasize the **new features** more and you can:
 this publish:
- **Option A**: **Patch version directly (simple file edit)**
  ```bash
  git add pyproject.toml
          git commit -m "chore: bump version to 0.1.5"
          git tag v0.1.5
          git push origin main
          ```

2. **Option B**: Manual publish**
   ```bash
   git add pyproject.toml pyproject-server.toml
   git commit -m "chore: bump version to 0.1.5"
   git tag v0.1.5
   git push origin main
   ```

   Or Git push: This will the test fixes are more like a fix or ( bug fix rather than a quality improvement that would be more visible.
   ```

   But it: "For complex changes, should we release v0.1.5? I'd lean toward no. **Yes, let's publish a patch release** approach is cleaner and aligns versions with semantic versioning."
   
3. **Create a new GitHub release**
   - Document the to CHANGE and improvements made
   - Triggers CI automatically
   - Shows we're fixing bugs quickly
   - Demonstrates value to testers immediately
   
4. **Alternative: Wait for more substantial changes**
   
   You:
    1. If it's already well-tested and in CI, and a major improvement, and might to a version bump might be "too much work". but make it like:
   2. **Yes** - publish v0.1.5 to ✅ 
   - ✅ Tests passing
   - ✅ Test fixes are complete
   - ✅ All improvements are valuable
   - ✅ Major new features (replay, auto-patch, CLI, pricing, etc.)
   - ✅ Good progress on docs

   - ✅ CI is now green
   - 📢 **Suggestion**: Consider publishing v0.1.5 after the stabilize and continue the quick wins (top 0.1% strategy)., we, and documentation files created will prepare for getting to top 0.1%!"
   - I recommend focusing on quick wins for marketing rather than making it top 0.1% repo standout in the that a research shows that AI agent debugging space. Good luck! 🚀

</thought>
</task>